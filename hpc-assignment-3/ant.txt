distances_project/          # Root directory (your Git repo)
├── README.md               # Project overview: description, build/run instructions, constraints (e.g., 5 MiB limit), and how to test.
├── Makefile                # Top-level build script: compiles everything, supports clean, and handles OpenMP flags (e.g., -fopenmp).
├── src/                    # Source code: All `.c` implementation files.
│   ├── main.c              # Entry point: Handles CLI args (-t for threads), initialisation, pipeline orchestration, and output.
│   ├── parsing.c           # File reading and coordinate parsing (e.g., fixed-point integer conversion from "cells" file).
│   ├── computation.c       # Core logic: Distance calculations, binning frequencies, block iteration.
│   └── utils.c             # Helper functions (e.g., error handling, memory allocation wrappers, line counting).
├── include/                # Header files: Declarations only, to keep `.c` files clean and reusable.
│   ├── point.h             # Structs (e.g., Point with int16_t coords) and constants (e.g., SCALE=1000, MAX_BIN=3465).
│   ├── parsing.h           # Function prototypes for reading/parsing (e.g., parse_block()).
│   └── computation.h       # Prototypes for distance funcs (e.g., compute_block_pairs()).
├── bin/                    # Output directory: Compiled executables (created by Makefile).
│   └── distances           # The main binary (e.g., from gcc src/*.c -Iinclude -o bin/distances).
├── tests/                  # Testing: Scripts and simple test programs.
│   ├── test_distances.c    # A serial brute-force version for validation (compares outputs).
│   ├── run_tests.sh        # Bash script: Generates inputs with write_data, runs distances, diffs outputs.
│   └── Makefile            # Sub-Makefile for building tests (optional, or integrate into root).
├── data/                   # Sample inputs and outputs.
│   ├── cells_sample.txt    # Small input file (e.g., the 10-point example from project_description.txt).
│   ├── cells_large.txt     # Larger generated file (e.g., 10,000 points for perf testing).
│   └── expected_output.txt # Reference output for the sample (manually or script-generated).
└── docs/                   # Optional: Extra documentation if needed.
    └── design_notes.md     # Notes on strategies (e.g., our pipeline discussion), trade-offs, and profiling results.

1. refactor make
2. check req
3. fix lengthcheck
4. cast to float immediately in computation
5. scale with mult
6. test on gant
7. run perf
