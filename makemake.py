from jinja2 import Environment, FileSystemLoader

# Find targets

# Look for build

# Boolean to make build if not present

# Search through targets dependencies

# List all files to compile

# Write the makefile